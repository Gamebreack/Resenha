# B-001: Project Scaffold

**Objective:** Create the Python project structure with packaging, testing, and environment management.

**Status:** done

**Notes:**
- `src/resenha/__init__.py` created with `__version__ = "0.1.0"`.
- `tests/__init__.py` created.
- `tests/test_version.py` created (TDD: red → green).
- Package installed in editable mode; `pytest tests/` passes (1 passed).

**Tasks:**
1. Create `pyproject.toml` — `resenha` package, Python 3.11+, dependencies: `discord.py`, `yfinance`, `feedparser`, `pydantic-settings`, `google-genai`, `requests`, `pytest`, `pytest-asyncio`.
2. Create `src/resenha/__init__.py` with `__version__ = "0.1.0"`.
3. Create `tests/__init__.py`.
4. Create `.gitignore` — ignore `.env`, `__pycache__/`, `*.pyc`, `.venv/`, `*.db`.
5. Create `.env.example` — list all required env vars without values.
6. Run `pip install -e .` and verify import `import resenha` succeeds.

**Acceptance Criteria:**
- `python -c "import resenha; print(resenha.__version__)"` outputs `0.1.0`.
- `pytest tests/` runs with 0 tests collected (no errors).
- `.env` exists with `DISCORD_BOT_TOKEN` set (not committed).
