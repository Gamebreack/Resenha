# B-007: News Fetcher

**Objective:** Aggregate financial news from RSS feeds for portfolio assets.

**Status:** done

**Notes:**
- `src/resenha/news.py` created with `NewsItem` dataclass and `fetch_news()`.
- Fetches InfoMoney, Suno, Seu Dinheiro RSS feeds.
- Filters by ticker mention, deduplicates by URL, caches per source (30min TTL).
- `tests/test_news.py` created with 5 tests.
- All 25 tests pass.

**Tasks:**
1. Create `src/resenha/news.py` — `fetch_news(tickers: list[str])` using `feedparser`.
   - Sources:
     - InfoMoney: `https://www.infomoney.com.br/feed/`
     - Suno Notícias: `https://sunoresearch.com.br/feed/`
     - Seu Dinheiro: `https://www.seudinheiro.com/feed/`
     - Exame: `https://exame.com/feed/` (check URL)
     - Google News RSS: construct per-ticker query URLs
   - Filter entries by ticker mention in title/summary (case-insensitive).
   - Deduplicate by URL.
   - Return: list of `NewsItem(title, url, source, published, summary, tickers_mentioned)`.
2. Cache in SQLite `news` table with 30min TTL per source.
3. Write `tests/test_news.py` — mock `feedparser.parse`, assert filtering and dedup.

**Acceptance Criteria:**
- Fetches from at least 3 RSS sources.
- Filters to only entries mentioning at least one portfolio ticker.
- Deduplicates by URL.
- Graceful on feed failure: skip source, log warning.
- All tests pass.
