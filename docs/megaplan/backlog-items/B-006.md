# B-006: Macro Fetcher

**Objective:** Fetch Brazilian macro indicators from BCB SGS API (no auth required).

**Status:** done

**Notes:**
- `src/resenha/macro.py` created with `MacroData` dataclass and `fetch_macro()`.
- Reuses `resenha.cache.Cache` with 1h TTL.
- `tests/test_macro.py` created with 5 tests.
- All 20 tests pass.
- Real BCB API verified: SELIC 14.5%, IPCA 0.67%, Dólar R$4.98.

**Tasks:**
1. Create `src/resenha/macro.py` — `fetch_macro()` using `requests`.
   - Endpoints:
     - SELIC (meta): `https://api.bcb.gov.br/dados/serie/bcdata.sgs.432/dados/ultimos/1?formato=json`
     - IPCA: `https://api.bcb.gov.br/dados/serie/bcdata.sgs.433/dados/ultimos/1?formato=json`
     - Dólar (PTAX): `https://api.bcb.gov.br/dados/serie/bcdata.sgs.1/dados/ultimos/1?formato=json`
   - Return typed dataclass: `MacroData(selic, ipca, dolar, fetched_at)`.
2. Cache in SQLite `macro` table with 1h TTL.
3. Write `tests/test_macro.py` — mock `requests.get`, assert correct parsing.

**Acceptance Criteria:**
- `fetch_macro()` returns `MacroData` with float values.
- Cache works (1h TTL).
- Graceful on API failure: returns `None` and logs warning.
- All tests pass.
