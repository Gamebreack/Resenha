# B-002: Config Loader

**Objective:** Load all runtime configuration from environment variables and validate with Pydantic.

**Status:** done

**Notes:**
- `src/resenha/config.py` created with `BaseSettings` and 8 fields.
- `src/resenha/__main__.py` created as sanity-check entrypoint.
- `tests/test_config.py` created with 2 tests (mock env + defaults).
- All 3 tests pass (including B-001's version test).

**Tasks:**
1. Create `src/resenha/config.py` — Pydantic `Settings` class with fields:
   - `discord_bot_token: str`
   - `discord_channel_id: int`
   - `gemini_api_key: str`
   - `google_sheet_id: str`
   - `google_sheet_tab: str = "Consolidado"`
   - `cache_db_path: Path = Path("data/resenha.db")`
   - `premarket_hour: int = 7`, `eod_hour: int = 19`
2. Create `src/resenha/__main__.py` — entrypoint that prints loaded config (sanity check).
3. Write test `tests/test_config.py` — mock env vars, assert all fields parse correctly.

**Acceptance Criteria:**
- `python -m resenha` prints config without errors.
- `pytest tests/test_config.py` passes (red → green).
- Missing required env var raises `ValidationError`.
