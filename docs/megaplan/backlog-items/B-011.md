# B-011: Summary Generator

**Objective:** Generate the final briefing text via Gemini, grouped by asset with bias annotation.

**Status:** done

**Notes:**
- `src/resenha/summary.py` created with `generate_briefing()` and `generate_embeds()`.
- `generate_embeds()` splits text by `## ` headers, creates Discord embeds, limits to 10 embeds × 4096 chars.
- `tests/test_summary.py` created with 3 tests.
- All 43 tests pass.

**Tasks:**
1. Create `src/resenha/summary.py`:
   - `generate_briefing(context: BriefingContext, gemini: GeminiClient, style: str)` → `str`:
     - Calls `build_briefing_prompt(context, style)`.
     - Calls `gemini.generate(prompt)`.
     - Returns raw LLM output.
   - `generate_embeds(briefing_text: str)` → `list[discord.Embed]`:
     - Splits briefing into sections per asset/topic.
     - Creates Discord embed per section (title + description).
     - Limits description to 4096 chars per embed (Discord limit).
     - Total embeds ≤ 10 (Discord limit).
2. Write `tests/test_summary.py`:
   - Mock GeminiClient and BriefingContext, assert prompt contains expected data.
   - Assert embed splitting handles long text correctly.
   - Assert embed count ≤ 10.

**Acceptance Criteria:**
- `generate_briefing()` returns non-empty string.
- `generate_embeds()` returns 1–10 discord.Embed objects.
- Each embed has title and description ≤ 4096 chars.
- All tests pass.
