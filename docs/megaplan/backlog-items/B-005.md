# B-005: Price Fetcher

**Objective:** Fetch current prices and historical data for portfolio assets via yfinance, cache to SQLite.

**Status:** done

**Notes:**
- `src/resenha/cache.py` created — reusable SQLite cache with TTL.
- `src/resenha/prices.py` created — `fetch_price()` with `.SA` suffix, yfinance integration, stale fallback.
- `tests/test_prices.py` (5 tests) and `tests/test_cache.py` (4 tests) created.
- All 15 tests pass.
- Real yfinance verified: ITUB4 at R$40.40, +1.94%.

**Tasks:**
1. Create `src/resenha/prices.py` — `fetch_price(ticker: str)` using `yfinance.Ticker`.
   - B3 tickers need `.SA` suffix for yfinance (e.g., `ITUB4.SA`).
   - Return: current price, day change %, volume, 52w high/low.
   - Cache results in SQLite to avoid repeated API calls within the same run.
2. Create `src/resenha/cache.py` — `Cache` class with SQLite connection.
   - Schema: `prices` table (ticker, price, change_pct, volume, high_52w, low_52w, fetched_at).
   - `get(ticker)` returns cached data if `fetched_at` within 1 hour.
   - `set(ticker, data)` writes fresh data.
3. Write `tests/test_prices.py` — mock `yfinance.Ticker`, assert correct `.SA` suffix, assert cache hit/miss logic.
4. Write `tests/test_cache.py` — test SQLite CRUD, TTL logic.

**Acceptance Criteria:**
- `fetch_price("ITUB4")` returns dict with price and change_pct.
- Cache hit within 1h returns cached data without hitting yfinance.
- All tests pass (red → green → blue).
