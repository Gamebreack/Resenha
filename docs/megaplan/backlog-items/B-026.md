# B-026: Expand RSS Sources — Tech, Finance, Politics, Culture

**Objective:** Add 13 verified, non-paywalled RSS feeds to Resenha across global tech/finance/politics and Brazilian culture, and raise the general-news item cap to match the prompt limit so the expanded diversity is actually used.

**Status:** COMPLETE

**Notes:**
- 13 feeds added to `GENERAL_RSS_SOURCES` in `src/resenha/news.py`.
- Two URLs were corrected after OpenCode's first pass: Rest of World (`/feed/latest/`) and Ars Technica (`/information-technology/feed/`).
- `fetch_general_news` cap raised from 50 to 70 to match `prompts.py`.
- `test_fetch_general_news_deduplicates` patched with `@patch.dict` to avoid hardcoding source count.
- All 119 tests pass.
- All new feeds verified reachable and returning items via `feedparser`.

**Research Artifacts:**
- [Finance/economics RSS research](../../research/rss/finance_rss_feeds.md)
- [Politics/current-affairs RSS research](../../research/rss/politics_rss_feeds.md)
- [Technology RSS research](../../research/rss/tech_rss_feeds_research.md)

**Tasks:**
1. Update `src/resenha/news.py`:
   - Append 13 new feeds to `GENERAL_RSS_SOURCES` with category comments:
     - Tech: Rest of World, TechCrunch, Ars Technica (Biz & IT), The Verge Tech.
     - Finance: The Guardian Business, CNBC Finance, NPR Economy, Fortune.
     - Politics: BBC News World, The Guardian World, Al Jazeera English, The American Conservative.
     - Culture: G1 Pop & Arte, Cinema com Rapadura, TMDQA!, Catraca Livre Gira.
   - Change `fetch_general_news` return from `items[:50]` to `items[:70]`.
2. Update `tests/test_news.py`:
   - Patch `GENERAL_RSS_SOURCES` in `test_fetch_general_news_deduplicates` to a deterministic 2-source dict so the test no longer hardcodes the source count.
   - Keep `test_fetch_general_news_per_source_limit` unchanged (it already patches to 2 sources).
3. Run the full test suite: `python -m pytest tests/ -q`.
4. Update this detail file and `backlog.md` to mark B-026 COMPLETE.

**Acceptance Criteria:**
- 13 new feeds are present in `GENERAL_RSS_SOURCES` with correct URLs.
- `fetch_general_news` returns up to 70 items.
- `test_fetch_general_news_deduplicates` passes without hardcoding source count.
- All pytest tests pass.
