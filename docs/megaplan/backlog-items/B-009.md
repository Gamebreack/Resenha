# B-009: Gemini Client

**Objective:** Integrate Google AI Studio (Gemini Flash) for text generation.

**Status:** done

**Notes:**
- `src/resenha/gemini.py` created with `GeminiClient` class.
- `src/resenha/prompts.py` created with `build_briefing_prompt()`.
- Tests created for both modules (5 tests total).
- All 36 tests pass.
- Uses `google-genai` SDK with model `gemini-2.0-flash-latest`.
- Retry on 429 ResourceExhausted, propagate other errors.

**Tasks:**
1. Create `src/resenha/gemini.py`:
   - `class GeminiClient`:
     - `__init__(api_key: str)` — initializes client.
     - `generate(prompt: str, max_tokens: int = 2000)` → `str` — sends prompt to Gemini Flash, returns generated text.
   - Use `google-genai` SDK. Model: `gemini-2.0-flash-latest`.
   - Handle API errors: retry once on 429, log and re-raise on other errors.
2. Create `src/resenha/prompts.py`:
   - `build_briefing_prompt(context: BriefingContext, style: str = "pre-market")` → `str`:
     - Assembles structured prompt from BriefingContext.
     - Includes voice instructions: relaxed, informal, Brazilian-Portuguese.
     - Includes bias annotation instruction.
     - Style can be "pre-market", "eod", or "saturday".
3. Write `tests/test_gemini.py`:
   - Mock `google-genai` client, assert correct model and prompt.
   - Assert retry on 429.
   - Assert error propagation on non-429.
4. Write `tests/test_prompts.py`:
   - Mock BriefingContext, assert prompt contains key tickers and macro data.
   - Assert voice instruction is present.

**Acceptance Criteria:**
- `generate()` returns non-empty string for valid prompt.
- Prompt builder produces structured text with all context fields.
- All tests pass (red → green → blue).
