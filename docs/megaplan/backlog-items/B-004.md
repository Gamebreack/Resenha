# B-004: Discord Bot Scaffold

**Objective:** Establish the Discord bot connection, verify channel access, and send a test embed.

**Status:** done

**Notes:**
- `src/resenha/delivery.py` created with `ResenhaBot` and `run_bot()`.
- `tests/test_delivery.py` created with mocked discord.py.
- All 6 tests pass.
- Real Discord verification: bot logged in as `ResenhaBot`, sent test embed to `#configured-channel`.

**Tasks:**
1. Create `src/resenha/delivery.py` — `ResenhaBot` class extending `discord.Client`.
2. Implement `send_briefing(channel_id, embeds)` async method.
3. Implement `on_ready` handler logging bot user + channel name.
4. Write test `tests/test_delivery.py` — mock `discord.Client`, assert `send_briefing` constructs embeds correctly.
5. Run manual smoke test: `python -m resenha --send-test` posts a "Resenha is online" embed to `#configured-channel`.

**Acceptance Criteria:**
- Bot logs in as `ResenhaBot` and confirms channel `configured-channel`.
- Test embed appears in `#configured-channel` with title "Resenha is online".
- `pytest tests/test_delivery.py` passes (red → green).
