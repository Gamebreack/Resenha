# B-019: General News Source Fixes

**Objective:** Remove broken UOL feed, add per-source limit of 5 for general news, and set global cap to 50.

**Status:** done

**Notes:**
- UOL removed. Per-source limit 5, global cap 50.
- 101 tests pass.

**Tasks:**
1. Remove UOL from `GENERAL_RSS_SOURCES` in `src/resenha/news.py`.
2. Modify `_fetch_from_sources()` or `fetch_general_news()` to apply per-source limit of 5 items before caching and dedup.
3. Change global cap in `fetch_general_news()` from `items[:20]` to `items[:50]`.
4. Update tests:
   - `tests/test_news.py`: verify per-source limit applies (mock 3 sources with 10 items each → max 15 total, 5 per source).

**Acceptance Criteria:**
- UOL removed from sources.
- Each general source contributes at most 5 items.
- Global general news cap is 50.
- All tests pass.
