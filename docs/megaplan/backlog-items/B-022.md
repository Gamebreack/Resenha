# B-022: Remove Suno RSS Source

**Objective:** Remove the Suno Notícias RSS feed from `RSS_SOURCES` because it consistently fails/crashes during fetch.

**Status:** done

**Notes:**
- Suno feed (`https://sunoresearch.com.br/feed/`) is unreliable and pollutes logs with repeated warnings.
- Removing it reduces noise and avoids wasted fetch attempts.

**Tasks:**
1. Remove the `"Suno"` entry from `RSS_SOURCES` in `src/resenha/news.py`.
2. Update any tests in `tests/test_news.py` that assume 3 financial sources (InfoMoney, Suno, Seu Dinheiro) to assume 2 sources (InfoMoney, Seu Dinheiro). Specifically update `test_fetch_news_cache_hit` assertion that `mock_parse.call_count == 3`.
3. Run full test suite.

**Acceptance Criteria:**
- `RSS_SOURCES` no longer contains Suno.
- All tests pass.
- No Suno references remain in source or tests.
