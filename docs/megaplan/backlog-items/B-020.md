# B-020: General News Prompt Limit

**Objective:** Increase the prompt's general news display limit from 5 to 15 so all fetched sources (G1, TecMundo, BBC Brasil) are visible to Gemini.

**Status:** done

**Notes:**
- `[:5]` → `[:15]` in prompts.py. Root cause: date-sorted G1 items always filled the old 5-slot limit.
- 101 tests pass.
- Root cause: `prompts.py` line 26 slices `general_news[:5]`. G1 timestamps are hours fresher than TecMundo/BBC, so G1 always fills all 5 slots. Gemini never sees other sources.
- Fetcher, caps, and assembler work correctly — 15 items (5 per source) land in `general_news`.
- Fix: `[:5]` → `[:15]`.

**Tasks:**
1. In `src/resenha/prompts.py`, change `context.general_news[:5]` to `context.general_news[:15]`.
2. Update `tests/test_prompts.py`: any assertion checking prompt output for 5 general items should now expect up to 15.

**Acceptance Criteria:**
- Prompt shows all general news items (up to 15).
- All tests pass.
