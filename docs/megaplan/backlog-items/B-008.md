# B-008: Raw Data Assembler

**Objective:** Combine price, macro, and news data into a structured context object for the LLM prompt.

**Status:** done

**Notes:**
- `src/resenha/assembler.py` created with `AssetPosition` and `BriefingContext` dataclasses.
- `assemble_briefing_data()` merges asset+price, groups news by ticker, separates general news.
- `tests/test_assembler.py` created with 6 tests.
- All 31 tests pass (full suite).

**Tasks:**
1. Create `src/resenha/assembler.py` — `assemble_briefing_data(assets, prices, macro, news)` → `BriefingContext`.
   - `BriefingContext` dataclass fields:
     - `portfolio: list[AssetPosition]` (asset + price data merged)
     - `macro: MacroData`
     - `news_by_asset: dict[str, list[NewsItem]]`
     - `general_news: list[NewsItem]` (news mentioning no specific asset, i.e., macro/sector)
     - `timestamp: datetime`
2. Merge logic:
   - For each asset, attach `price_data` from prices module.
   - Group news by which tickers they mention. News mentioning multiple tickers appears in all.
   - News with no ticker mentions goes to `general_news`.
3. Write `tests/test_assembler.py` — mock inputs, assert correct grouping and structure.

**Acceptance Criteria:**
- All 11 assets have price data attached (or `None` if fetch failed).
- News correctly grouped by ticker and general bucket.
- `BriefingContext` is JSON-serializable (for potential logging/debugging).
- All tests pass.
