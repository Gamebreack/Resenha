# B-003: Portfolio Reader

**Objective:** Read the live portfolio from Google Sheets and parse into typed dataclasses.

**Status:** done

**Notes:**
- `src/resenha/portfolio.py` created with `Asset` dataclass and `read_portfolio()`.
- `tests/test_portfolio.py` created with mocked Sheets API.
- All 4 tests pass.
- Real API verified: 11 assets parsed correctly from "Consolidado" tab.
- Numeric parsing handles Brazilian format (R$ prefix, comma decimals, dot thousands).

**Tasks:**
1. Create `src/resenha/portfolio.py` — `Asset` dataclass with fields: ticker, sector, target_pct, qty, avg_price, current_price, invested, value, pnl, return_pct, weight_pct, distance_meta, status.
2. Implement `read_portfolio(sheet_id, tab_name)` → `list[Asset]` using `googleapiclient` + token from `the configured Google OAuth token file`.
3. Handle numeric parsing: strip `R$ `, convert percentage strings to floats, map empty cells to `None`.
4. Write test `tests/test_portfolio.py` — mock Sheets API response, assert 11 assets parsed with correct types.

**Acceptance Criteria:**
- `python -m resenha` prints all 11 assets with correct tickers and sectors.
- `pytest tests/test_portfolio.py` passes (red → green).
- Empty or malformed rows are skipped gracefully.
