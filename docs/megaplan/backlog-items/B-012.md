# B-012: Discord Embed Formatter

**Objective:** Enhance embed generation with categorized colors, branding, timestamps, and add retry logic to delivery.

**Status:** done

**Notes:**

**Tasks:**
1. Enhance `generate_embeds()` in `src/resenha/summary.py`:
   - Category-based embed colors: macro (0x2ECC71 green), portfolio (0x3498DB blue), news (0xE67E22 orange), default (0x00AAFF).
   - Detect category by section header keywords: "Macro", "Carteira", "Portfolio", "IBOV" → portfolio; "SELIC", "IPCA", "Dólar", "Câmbio" → macro; everything else → news.
   - Add `discord.Embed.timestamp` set to current UTC.
   - Add `set_footer(text="Resenha · Seu briefing diário")`.
   - Add `set_author(name="Resenha")`.
   - Preserve existing section-splitting logic and 10-embed limit.
2. Enhance `send_briefing()` in `src/resenha/delivery.py`:
   - Add retry with exponential backoff: 3 attempts, 2s/4s/8s delays.
   - Log each retry attempt at WARNING level.
   - Raise last exception after exhausting retries.
3. Write tests in `tests/test_summary.py`:
   - Test category color assignment: macro section → green, portfolio → blue, news → orange.
   - Test embed has timestamp and footer.
   - Test embed has author "Resenha".
4. Write tests in `tests/test_delivery.py`:
   - Test retry: mock `channel.send` to fail twice then succeed, assert 3 attempts.
   - Test retry exhaustion: mock `channel.send` to fail 3 times, assert exception raised.

**Acceptance Criteria:**
- Embeds have correct color per category.
- Embeds have timestamp, footer, and author.
- `send_briefing()` retries up to 3 times with backoff.
- All existing + new tests pass.
